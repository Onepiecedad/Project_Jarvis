# Use the official Agent Zero image directly
# All customizations will be done via environment variables and runtime config
FROM agent0ai/agent-zero:latest

# Railway uses PORT env variable, Agent Zero uses port 80
# We'll set up the port mapping in railway.toml

# The base image already has everything configured
# No changes needed - let it run as-is

EXPOSE 80
